---
title: Endpoints Overview
description: REST API endpoints for MultiTenantKit
lastUpdated: '2025-12-09'
---

# Endpoints Overview

MultiTenantKit provides a complete REST API with 18 endpoints for managing users, organizations, and memberships. Each endpoint corresponds to a use case and follows REST conventions.

## Authentication

All endpoints require authentication via JWT token in the `Authorization` header:

```
Authorization: Bearer YOUR_JWT_TOKEN
```

The `principalExternalId` (user's auth provider ID) is automatically extracted from the JWT and passed to each use case.

## Setup Options

MultiTenantKit supports two deployment targets with the same endpoints:

### Express (Node.js)

**New app** - Create a complete Express application:

```typescript
import { createSupabaseExpressApp } from '@multitenantkit/sdk';

const app = createSupabaseExpressApp();
app.listen(3000);
// Endpoints: /users/me, /organizations, etc.
```

**Existing app** - Add routes to your Express application:

```typescript
import express from 'express';
import { createExpressRouter } from '@multitenantkit/sdk';

const app = express();

// Your existing routes
app.get('/api/billing', billingHandler);

// Add MultiTenantKit under a prefix
app.use('/api/teams', createExpressRouter());
// Endpoints: /api/teams/users/me, /api/teams/organizations, etc.

app.listen(3000);
```

### Supabase Edge Functions

Deploy as a serverless function on Supabase's edge network:

```typescript
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
import { createSupabaseEdgeHandler } from 'https://esm.sh/@multitenantkit/sdk-supabase';

const client = createClient(
  Deno.env.get('PROJECT_URL')!,
  Deno.env.get('SERVICE_ROLE_KEY')!
);

const handler = createSupabaseEdgeHandler({
  client,
  edgeOptions: { basePath: '/multitenantkit' }
});

Deno.serve(handler);
// Endpoints: /multitenantkit/users/me, /multitenantkit/organizations, etc.
```

<Callout type="info">
Both deployment options expose the same 18 endpoints with identical behavior. Choose based on your infrastructure preferences.
</Callout>

## Response Format

All successful responses follow this format:

```json
{
  "data": { /* Response data */ },
  "requestId": "uuid-v4"
}
```

Error responses follow this format:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable error message"
  },
  "requestId": "uuid-v4"
}
```

## Endpoints by Category

### User Endpoints
- `GET /users/me` - Get authenticated user
- `PATCH /users/me` - Update user
- `DELETE /users/me` - Delete user
- `GET /users/me/organizations` - List user's organizations

### Organization Endpoints
- `POST /organizations` - Create organization
- `GET /organizations/:organizationId` - Get organization
- `PATCH /organizations/:id` - Update organization
- `DELETE /organizations/:organizationId` - Delete organization
- `POST /organizations/:organizationId/restore` - Restore organization
- `GET /organizations/:organizationId/members` - List organization members
- `POST /organizations/:organizationId/transfer-ownership` - Transfer ownership

### Membership Endpoints
- `POST /organizations/:organizationId/members` - Add member
- `PUT /organizations/:organizationId/members/:userId/role` - Update member role
- `DELETE /organizations/:organizationId/members/:userId` - Remove member
- `POST /organizations/:organizationId/leave` - Leave organization

## Learn More

- [User Endpoints](/docs/endpoints/user-endpoints)
- [Organization Endpoints](/docs/endpoints/organization-endpoints)
- [Membership Endpoints](/docs/endpoints/membership-endpoints)
